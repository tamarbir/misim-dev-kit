# צד לקוח — Angular 20

מסך יצירת טופס: Reactive Forms, FormArray לשדות ולאבני דרך, ו-signals למצב מול ה-API.

```bash
npm start
```

הדפדפן: http://localhost:43143  
בקשות `/api` מועברות לשרת ב-http://localhost:43142
